package com.specproject.backend.domain.schedule;

import org.springframework.data.jpa.repository.JpaRepository;

public interface Schedule_TodoRepository extends JpaRepository<Schedule_Todo, Long> {
}
